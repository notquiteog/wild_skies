return {
  summary   = "Flying Pokemon and connected flocks across Gen1, Gen2 and native Gen3. Independent ecology with optional Ride, Doubles and Online integration.",
  author    = "Shane Hudson",
  tags      = { "overworld", "ambient", "cosmetic", "lets-go" },
  differences = {
    changed = { "captured native doubles partners stay removed when the engine promotes the second foe into its capture slot" },
    added = {
      "FLYING species from the map's grass slots fly across with a shadow",
      "ambient skies derive from the world's encounter tables, so dataset overhauls (Crystal 251) fill the air with their own species automatically",
      "loads on Gen 2 (Gold): Johto's per-time-of-day tables feed the sky, birds wear their species' overworld icon",
      "native Gen3 supports all five saved options, OPTIONS and SKY DEX, species art, neighboring flocks, ground perches and aerial encounters",
      "optional Online SKY claims preserve host identities through battles and restore survivors without guest duplication",
    },
    known = {
      "icon-only classes (bug/plant/quadruped/snake) ride the bird look",
      "bold low birds support ground battles; optional Ride provides aerial interception",
      "voxel mode shows flyers as billboards with altitude (lightly tested)",
      "all three carts pass boot/settings checks; full-map/controller/network coverage remains incomplete, and aerial claims require Ride's event-namespace fix",
      "native Gen3 uses collision-safe ground perches; rooftop perches await a renderer-independent public roof query",
      "native Gen3 ambient fallback pools are curated FRLG species, rather than the legacy world-wide encounter-frequency model",
    },
  },
  credits = {},
  compat  = { engine = "0.0.0-dev || >=0.1.0 <2.0.0", modApi = 2 },
}
